<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4 col-sm-offset-4 form-n">
      <!-- Insert form here -->
      <div class="form-new">
      <h3>New Bid</h3>

      <%= simple_form_for [@auction, @bid] do |f| %>
        <div class="form-inputs">
          <%= f.input :content %>
        </div>
        <div class="form-actions">
          <%= f.button :submit %> <i class="fas fa-undo back-i"><%= link_to 'Back', dashboard_path, class: "back" %></i>
        </div>
          <% end %>
        </div>
    </div>
  </div>
</div>

