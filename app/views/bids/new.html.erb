<h3 class="text-center">New Bid</h3>

<%= simple_form_for [@auction, @bid] do |f| %>
  <div class="form-inputs">
    <%= f.input :content %>
  </div>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%= link_to 'Back', dashboard_path %>
