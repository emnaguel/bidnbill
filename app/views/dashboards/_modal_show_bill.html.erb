<div class="modal fade auction-<%= auction.id %> text-center" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
        <div class = "text-center first"><%= auction.client.first_name%> <%= auction.client.last_name%></div>
        <div class = "text-center"><%= auction.bill.category.name%></div>
        <div class = "text-center">Number: <%= auction.bill.id%></div>
        <div class = "text-center">Address: <%= auction.bill.address%></div>
        <div class = "text-center"><%= auction.bill.price%></div>
        <div class = "text-center"><%= auction.bill.price%></div>
        <div class = "text-center">€<%= auction.category.price_cents%></div>

        <% if policy(Bid).create? %>
          <%= link_to "Create a bid", new_auction_bid_path(auction), class: "btn btn-info up create" %>
        <% end %>

        <% if policy(auction.bill).show? %>
          <%= link_to "Back to bill number #{@auction.bill.id}", bill_path(@auction.bill) %> |
        <% end %>
          <%= link_to 'Back to dashboard', dashboard_path %>
    </div>
  </div>
</div>
